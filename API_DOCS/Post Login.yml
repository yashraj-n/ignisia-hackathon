info:
  name: Post Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9000/api/login
  body:
    type: json
    data: |
      {
          "login_email": "ynnarke52@gmail.com",
          "login_password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
